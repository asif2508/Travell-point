import React from 'react';
import Header from '../Header/Header';

const Register = () => {
    return (
        <div id='register' >
            <Header></Header>
        </div>
    );
};

export default Register;